# Project Status

## Current Phase
**Phase 1: MVP Complete**

All v0.1 application code written and tested. MCP server, CLI, and core library are ready for hardware validation.

---

## Technical Debt & Known Issues

- CLI `connect`/`query`/`write` subcommands open and close a session per invocation. This is intentional for debug UX but means the session is not persistent across CLI calls (only across MCP calls). Not a concern for v0.1.

---

## Recent History

- **2026-05-27** — **[Feature]** Added per-instrument memory file. `load_instrument_memory(alias)` added to `config.py` — reads `~/.agentlink/instruments/<alias>.md`, returns content or None. `connect()` response now includes `instrument_memory` field. `run_diagnostics()` now includes `instrument_memory` in `alias_check` when config is valid, so quirk history is available during troubleshooting. Added `## Instrument Memory` section to `_INSTRUCTIONS` with format spec (compact one-line bullets, `## category` headers, write-only-on-meaningful-troubleshooting rule). Updated `connect_instrument` and `diagnose_connection` tool docstrings. 7 new tests. 44/44 passing.
- **2026-05-27** — **[Feature]** Added SCPI transaction logging and VISA/SCPI agent context. (1) New `agentlink/scpi_logger.py`: `log_event(**fields)` appends JSONL entries to `~/.agentlink/logs/YYYY-MM-DD.jsonl`. Default-on; disable by setting `AGENTLINK_LOG_DIR=""`. Every connect/disconnect/query/write call — success and failure — logs op, alias, command, response/error. Never raises. (2) Modified `agentlink/tools.py`: `log_event` called at every return point across all four tools. (3) Added `## VISA/SCPI Behavior` section to `_INSTRUCTIONS` in `mcp_server.py`: covers write-is-fire-and-forget, three distinct timeout causes with differential diagnosis, `****` response meaning, parallel query efficiency, and log location. (4) Added `tests/conftest.py` autouse fixture to disable logging during test runs. (5) Added 8 `TestScpiLogger` tests. Updated `system_architecture.md` (new component E, env var table) and `agent_development.md` (§5 interface-specific context pattern). 37/37 tests passing.

- **2026-05-26** — **[Bugfix + Feature]** Pre-hardware polish pass. (1) Fixed session leak in `connect()`: if `*IDN?` raises after `open_session()` succeeds, the orphaned session is now cleaned up so the alias does not get stuck. (2) Fixed `agentlink list` stdout/stderr bug: all output now goes to stdout (was: alias on stdout, everything else on stderr). Refactored to single-pass loop over TOML files. (3) Removed dead `QueryError` exception (was defined but never raised or caught). (4) Added `agentlink/diagnostics.py`: `run_diagnostics(alias=None)` checks pyvisa/pyvisa-py versions, VISA backend health, `list_resources()` output, interface-type breakdown (USB/GPIB/TCPIP/serial), config directory status, and alias-specific reachability (USB in resource list, TCPIP ping + port 5025, GPIB adapter detection). Returns `{ready, action_items, ...}`. Exposed as `diagnose_connection` MCP tool and `agentlink diagnose [alias]` CLI command. Updated `_INSTRUCTIONS` in `mcp_server.py` to direct agent to run diagnostics first. 29/29 tests passing.
- **2026-05-26** — **[UX/Docs]** Added MCP server-level `instructions` to `mcp_server.py` (FastMCP `instructions=` param) directing the agent to own instrument config creation and `techmanual_document_id` discovery. Updated `connect_instrument` docstring to reinforce the same. Created `server.json` (MCP registry manifest, packages-only/stdio, no remotes). Rewrote `README.md`: pip-based install, `"command": "agentlink-mcp"` MCP config pattern (mirrors techmanual client), techmanual.ai framed as optional complement, fixed all UX issues from audit (mkdir, termination defaults, resource string output explanation, verify step, VISA troubleshooting section, removed TMAI_API_KEY from env table).
- **2026-05-26** — **[Bugfix/Polish]** Pre-hardware audit pass. Fixed CLI double command registration (commands now registered once via `name=` arg on decorator, removed redundant `cli.add_command` block). Fixed `ResourceManager` leak in `session.py` (now cached as module-level singleton). Fixed silent double-connect overwrite: `connect()` returns a structured error if a session is already open for the alias, guarded by new `session.is_connected()` helper. Added broken-config warnings to `agentlink list` — files that fail validation now print to stderr instead of silently disappearing. Added `pytest>=8.0` as `[dev]` optional dependency in `pyproject.toml`. Updated README: `uv pip install -e .` step added, `uv run python -c ...` for resource string discovery, MCP setup now covers both Claude Code (`.mcp.json`) and Claude Desktop (`claude_desktop_config.json`) with reliable `uv --directory` invocation pattern. 17/17 tests passing.
- **2026-05-26** — **[MVP]** Full v0.1 implementation complete. Built: `agentlink/exceptions.py`, `agentlink/config.py`, `agentlink/session.py`, `agentlink/tools.py`, `mcp_server.py`, `cli.py`, `tests/test_tools.py`, `examples/instruments/example_scope.toml`, `pyproject.toml`, `requirements.txt`, `.env.example`, `README.md`. 16/16 unit tests passing (all mocked, no hardware required).
- **2026-05-26** — **[Bootstrap]** Repository created. `agent-bootstrap.md` written by lead developer establishing founding design decisions. `docs/agent_docs/` scaffolded: `readme_agent.md`, `project_goal.md`, `agent_development.md`, `current_status.md`, `system_architecture.md`. No application code yet.
